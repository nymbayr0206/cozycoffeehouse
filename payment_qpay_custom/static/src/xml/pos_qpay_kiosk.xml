<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-inherit="pos_self_order.PaymentPage" t-inherit-mode="extension">
        <xpath expr="//div[@t-if='state.selection']" position="replace">
            <div t-if="state.selection" class="grid gap-4 align-items-center justify-content-center p-3">
                <button
                    t-foreach="availablePaymentMethods"
                    t-as="payment_method"
                    t-key="payment_method.id"
                    role="button"
                    class="g-col-12 g-col-md-6 btn btn-light btn-lg d-flex flex-column align-items-center gap-2 px-0 py-5 border border-light shadow-sm overflow-hidden"
                    t-on-click="() => this.selectMethod(payment_method.id)"
                >
                    <i class="fa fa-credit-card" aria-hidden="true" />
                    <span t-esc="payment_method.name" />
                </button>
            </div>
        </xpath>

        <xpath expr="//div[hasclass('payment-state-container')]" position="replace">
            <t t-if="!state.selection and isQPaySelected and state.qpayLoading">
                <div class="payment-state-container d-flex flex-column gap-4 align-items-center justify-content-center w-75 mx-auto p-3">
                    <h1 class="mb-2 text-center">Preparing QPay QR code...</h1>
                    <div class="spinner-border text-primary" style="width: 4rem; height: 4rem;" role="status">
                        <span class="visually-hidden">Loading...</span>
                    </div>
                </div>
            </t>

            <t t-elif="!state.selection and isQPaySelected and state.qpayQR">
                <div class="payment-state-container d-flex flex-column gap-3 align-items-center justify-content-center w-75 mx-auto p-3">
                    <h1 class="mb-1 text-center">Pay with QPay</h1>
                    <p class="text-muted text-center mb-0">
                        Scan this QR code with your QPay app.
                    </p>
                    <div class="border border-2 rounded p-2 bg-white shadow-sm">
                        <img
                            t-att-src="'data:image/png;base64,' + state.qpayQR"
                            alt="QPay QR code"
                            style="width: 260px; height: 260px; object-fit: contain;"
                        />
                    </div>
                    <a
                        t-if="state.qpayShortUrl"
                        t-att-href="state.qpayShortUrl"
                        class="btn btn-outline-primary btn-sm"
                        target="_blank"
                        rel="noopener noreferrer"
                    >
                        Open QPay link
                    </a>
                    <p class="text-muted small text-center mt-1 mb-0">
                        The kiosk will continue automatically after the payment is confirmed.
                    </p>
                </div>
            </t>

            <t t-elif="!state.selection and isQPaySelected and state.qpayError">
                <div class="payment-state-container d-flex flex-column gap-4 align-items-center justify-content-center w-75 mx-auto p-3">
                    <h1 class="mb-2 text-center text-danger">QPay Error</h1>
                    <p class="text-center mb-0" t-esc="state.qpayError" />
                </div>
            </t>

            <t t-else="">
                <div class="payment-state-container d-flex flex-column gap-4 align-items-center justify-content-center w-50 mx-auto p-3">
                    <h1 class="mb-3 text-center">Follow instructions on the terminal</h1>
                    <div class="btn btn-light btn-lg w-100 py-5 border border-light shadow-sm">
                        <i class="fa fa-credit-card-alt fa-3x" aria-hidden="true" />
                    </div>
                </div>
            </t>
        </xpath>

        <xpath expr="//div[hasclass('o_self_footer')]" position="replace">
            <div class="o_self_footer container o_self_container d-flex justify-content-between align-items-center position-relative py-3">
                <button
                    class="btn btn-secondary btn-lg"
                    t-if="state.selection or (selectedPaymentMethod and selectedPaymentMethod.is_online_payment) or isQPaySelected or selfOrder.paymentError"
                    t-on-click="back"
                >
                    Back
                </button>
                <button
                    class="btn btn-primary btn-lg"
                    t-if="!state.selection and (selfOrder.paymentError or state.qpayError)"
                    t-on-click="startPayment"
                >
                    Retry
                </button>
            </div>
        </xpath>
    </t>
</templates>
